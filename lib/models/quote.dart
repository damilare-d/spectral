class Quote {
  final String text;
  final int startMs;
  final int endMs;
  final double score;

  const Quote({
    required this.text,
    required this.startMs,
    required this.endMs,
    required this.score,
  });
}
